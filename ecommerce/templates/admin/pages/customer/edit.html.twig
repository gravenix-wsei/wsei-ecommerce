{% extends 'admin/base.html.twig' %}

{% block title %}Edit Customer - Admin Panel{% endblock %}

{% block content %}
<div class="admin-header">
    <h1>Edit Customer: {{ customer.fullName }}</h1>
    <a href="{{ path('admin.customer.index') }}" class="btn btn-secondary">
        {{ svg('img/icons/arrow-left.svg', {class: 'icon'}) }}
        Back to list
    </a>
</div>

<div class="form-container">
    {{ include('admin/pages/customer/_form.html.twig') }}
</div>
{% endblock %}

