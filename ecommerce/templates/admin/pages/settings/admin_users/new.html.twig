{% extends 'admin/pages/settings/base.html.twig' %}

{% block title %}Add New Administrator{% endblock %}

{% block settings_title %}Add New Administrator{% endblock %}

{% block settings_content %}
<div class="admin-container">
    <div class="card">
        {{ form_start(form) }}

            <div class="form-group">
                {{ form_label(form.email) }}
                {{ form_widget(form.email) }}
                {{ form_errors(form.email) }}
            </div>

            <div class="form-group">
                {{ form_label(form.plainPassword) }}
                {{ form_widget(form.plainPassword) }}
                {{ form_errors(form.plainPassword) }}
                <small class="form-text">Password must be at least 8 characters long.</small>
            </div>

            <div class="form-group">
                {{ form_label(form.roles) }}
                {{ form_widget(form.roles) }}
                {{ form_errors(form.roles) }}
                {{ form_help(form.roles) }}
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary">
                    {{ svg('img/icons/check.svg', {class: 'icon'}) }}
                    Create Administrator
                </button>
                <a href="{{ path('admin.settings.admin_users') }}" class="btn btn-secondary">Cancel</a>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}

